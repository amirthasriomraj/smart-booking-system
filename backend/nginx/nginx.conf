events {}

http {
    server {
        listen 80;

        # Frontend
        location / {
            proxy_pass http://frontend:5173;
        }

        # Backend API
        location /api {
            proxy_pass http://backend:8000;
        }
    }
}